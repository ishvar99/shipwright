# better-auth declares optional peers for frameworks we do not use (SvelteKit, Prisma,
# drizzle...). npm still tries to resolve them and collides with vitest's vite. These peers
# are marked optional upstream, so skipping the check is correct rather than a workaround.
legacy-peer-deps=true
